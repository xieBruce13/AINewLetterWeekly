# Newsletter HTML renderer (tools/convert_to_pdf.py)
markdown>=3.4

# DB sync bridge (tools/sync_to_db.py — pushes a finished run to Postgres)
psycopg[binary]>=3.2
requests>=2.31

# Pipeline runner (tools/run_pipeline.py, ai_filter.py): OpenAI Chat
# Completions for enrichment AND embeddings for the publisher.
openai>=1.40

# Source registry (tools/scraper.py reads tools/sources.yaml)
pyyaml>=6.0
